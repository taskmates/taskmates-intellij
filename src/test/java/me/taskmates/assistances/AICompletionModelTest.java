package me.taskmates.assistances;

import junit.framework.TestCase;

public class AICompletionModelTest extends TestCase {

    public void testAll() {
        // assertTrue(TextCompletionSSERequest.all("code-completion").size() > 0);
        // assertTrue(TextCompletionSSERequest.all("text-completion").size() > 0);
    }
}
